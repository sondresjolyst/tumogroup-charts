apiVersion: v1
kind: Service
metadata:
  name: {{ include "nstuning-api.fullname" . }}
  labels:
    {{- include "nstuning-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 7297
      protocol: TCP
      name: http
  selector:
    {{- include "nstuning-api.selectorLabels" . | nindent 4 }}
