apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyttogpanne-api.fullname" . }}
  labels:
    {{- include "pyttogpanne-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 7297
      protocol: TCP
      name: http
  selector:
    {{- include "pyttogpanne-api.selectorLabels" . | nindent 4 }}
