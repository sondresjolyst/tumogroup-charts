{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "altinnendata-api.fullname" . }}
  labels:
    {{- include "altinnendata-api.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "altinnendata-api.selectorLabels" . | nindent 6 }}
{{- end }}
