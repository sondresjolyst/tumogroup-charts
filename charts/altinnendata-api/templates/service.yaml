apiVersion: v1
kind: Service
metadata:
  name: {{ include "altinnendata-api.fullname" . }}
  labels:
    {{- include "altinnendata-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 7297
      protocol: TCP
      name: http
  selector:
    {{- include "altinnendata-api.selectorLabels" . | nindent 4 }}
