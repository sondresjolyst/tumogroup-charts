apiVersion: v1
kind: Service
metadata:
  name: {{ include "altinnendata-app.fullname" . }}
  labels:
    {{- include "altinnendata-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "altinnendata-app.selectorLabels" . | nindent 4 }}
