apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bobby-api.fullname" . }}-test-connection"
  labels:
    {{- include "bobby-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bobby-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
