{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "bobby-api.fullname" . }}
  labels:
    {{- include "bobby-api.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "bobby-api.selectorLabels" . | nindent 6 }}
{{- end }}
