apiVersion: v1
kind: Service
metadata:
  name: {{ include "bobby-api.fullname" . }}
  labels:
    {{- include "bobby-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 7297
      protocol: TCP
      name: http
  selector:
    {{- include "bobby-api.selectorLabels" . | nindent 4 }}
