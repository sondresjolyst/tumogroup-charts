{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "nstuning-app.fullname" . }}
  labels:
    {{- include "nstuning-app.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "nstuning-app.selectorLabels" . | nindent 6 }}
{{- end }}
