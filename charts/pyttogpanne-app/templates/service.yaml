apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyttogpanne-app.fullname" . }}
  labels:
    {{- include "pyttogpanne-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "pyttogpanne-app.selectorLabels" . | nindent 4 }}
