apiVersion: v1
kind: Service
metadata:
  name: {{ include "sjolystinnovation-app.fullname" . }}
  labels:
    {{- include "sjolystinnovation-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "sjolystinnovation-app.selectorLabels" . | nindent 4 }}
