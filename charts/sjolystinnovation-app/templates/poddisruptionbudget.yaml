{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "sjolystinnovation-app.fullname" . }}
  labels:
    {{- include "sjolystinnovation-app.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "sjolystinnovation-app.selectorLabels" . | nindent 6 }}
{{- end }}
